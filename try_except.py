#   try and except is basically used as the default in c , in switch case
#   it's use to 
try :
    x = int(input("value : "))
    print(x)
except :
    print("INVALID CHOICE")

#   so if anything goes wrong in try , there will be except as the back up
